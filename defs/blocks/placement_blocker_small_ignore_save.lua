
local data = {
	width = 90,
	height = 90,
	blockTypes = {"placement"},
	canBeMoved = false,
	drawLayer = 20,
	ignoreSave = true,
	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)
	end,
}

return data
