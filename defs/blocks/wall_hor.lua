
local data = {
	width = 160,
	height = 40,
	blockTypes = {"ant", "placement", "flying"},
	canBeMoved = false,
	drawLayer = 250,
	
	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)
	end,
}

return data
