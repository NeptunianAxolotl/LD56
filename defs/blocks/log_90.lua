
local data = {
	width = 200,
	height = 100,
	blockTypes = {"ant", "placement"},
	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)

	end,
}

return data
