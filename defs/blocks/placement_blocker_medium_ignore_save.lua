
local data = {
	width = 100,
	height = 100,
	blockTypes = {"placement"},
	canBeMoved = false,
	drawLayer = 20,
	ignoreSave = true,
	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)
	end,
}

return data
