
local data = {
	width = 160,
	height = 160,
	blockTypes = {"placement"},
	canBeMoved = false,
	drawLayer = 20,
	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)
	end,
}

return data
