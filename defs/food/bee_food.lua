
local data = {
	foodType = "nectar",
	foodValue = 1,
	scentRadius = false,
	scentStrength = false,
	closestDistScale = 0.7,
	ignoreSave = true,

	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)
	end,
}

return data
