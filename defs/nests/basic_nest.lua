
local data = {
	spawnFrequency = 6,
	antType = "ant",
	health = 50,
	init = function (self)
	end,
	update = function (self, dt)
	end,
	draw = function (self, drawQueue)
	end,
}

return data
